import { Form } from "../../components/Form/Form";
import styles from "./Login.module.scss";

export const Login = () => {
  return (
    <div className={styles.login}>
      <Form />
    </div>
  );
};
